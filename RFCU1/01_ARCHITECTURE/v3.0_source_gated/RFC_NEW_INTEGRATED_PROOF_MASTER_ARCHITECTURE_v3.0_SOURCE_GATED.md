# RFC New Integrated Proof — Master Architecture v3.0

**Status:** source-gated canonical candidate  
**Parent:** the three canonical proofs and the user’s current locks, once directly verified  
**Not a parent:** RFCU2 execution state, its Module B chain, any tuned packet, or any old failure  
**Scientific runs authorized:** no  
**Immediate purpose:** define the complete proof before any scientific execution begins

## 1. Outcome

This architecture returns the work to its actual objective:

> Construct a brand-new RFC proof in which the completed triad kernel generates a physically realized, cyclic universe; the revised N-body proof enhances the kernel at its terminal end; the Big Implosion is the first physical event; the Big Rip ends a universe and prepares the next cycle; quantitative predictions are derived before data exposure; and the strongest justified claims are made without fitting, retuning, post-hoc repair, or inherited failure.

The architecture is intentionally a proof design, not a run-management system. It has one dependency graph, one source firewall, one theorem spine, and one corrected A–Q ownership map.

It is source-gated because several canonical files are hash-identified but not directly readable in the current workspace. No scientific module may execute until those exact bytes are verified and this architecture is promoted from **candidate** to **canonical**.

## 2. The final theorem target

The proof is designed to establish a theorem of the following form.

### Integrated RFC Universe Theorem — target

Given:

1. the ordered primitive triad \((\mathrm{CIF},\mathrm{QV},\mathrm{RFL})\);
2. the First Action \(\mathrm{QV}(\mathrm{CIF})\to\mathrm{RFL}\);
3. the source-owned base-kernel constitution;
4. the revised N-body terminal completion, activation, specialization, memory, and no-loss rules;
5. a source-derived minimal physical interface;
6. all later laws derived in the target-blind order fixed below;

prove that every lawful manifested RFC universe history is generated by a specialization of the completed kernel

\[
\widehat{\mathcal K}_N
=\operatorname{Complete}_{NB}
\!\left(K_f,\mathbf C_N,\mathsf{Act},\mathsf{Spec}\right),
\]

that its first physical event is the Big Implosion, that its ordinary and dark sectors descend through distinct lawful branches of that event, that its physical evolution preserves the proved kernel invariants, and that its terminal Big Rip exports only qualified memory into the next effective CIF before a new First Action and Big Implosion begin the next universe.

The theorem has two conclusions that must remain visibly distinct:

- **Internal conclusion:** the full manifested universe and its cycle follow from the admitted RFC axioms and derived laws.
- **Empirical conclusion:** the frozen quantitative consequences survive predeclared comparison with public observations.

The first is not weakened merely because the second is tested later. The second is not claimed until the blind empirical module passes.

## 3. Non-negotiable locks

1. **Ordered primitives:** CIF, QV, and RFL are primitive and role-distinct.
2. **First Action:** `QV(CIF) → RFL`.
3. **No background physical time:** physical time must be generated from QV-driven ordered persistent change.
4. **Terminal integration:** the N-body machinery completes the terminal end of the base kernel by typed composition.
5. **No duplicate dynamics:** no later module may add a second N-body sum or a parallel N-body universe.
6. **Universal inheritance:** every applicable later specialization receives the completed kernel.
7. **Dormancy:** inapplicable carrier components remain dormant with exactly zero backreaction.
8. **Witness activation:** a route becomes active only through its admitted witness and specialization certificate.
9. **No loss:** promotion, refinement, closure, and reopening preserve all protected prior information.
10. **Exact growth:** adding one body adds exactly \(2N\) directed lanes.
11. **Finite unboundedness:** arbitrary finite manifestation is proved without assuming a physically completed infinite-\(N\) state.
12. **First physical event:** the Big Implosion is the sole minimal physical event.
13. **Dark-sector distinction:** dark matter and dark energy share ancestry but do not share an identity or an automatically shared law.
14. **Cycle closure:** the Big Rip is the terminal physical event of a universe and the restart boundary for the next cycle.
15. **Object distinction:** RFL state, persistent memory, and the next effective CIF are not interchangeable.
16. **No inherited failure:** an old failure may define a test or forbidden route, never a premise.
17. **No fit, retune, or recovery:** no public target or historical coefficient may generate a formula.
18. **Calibration separation:** internal physicalization, absolute-unit calibration, and empirical comparison are separate proof stages.
19. **Strong claims:** each result is stated at the highest claim level actually established.
20. **No runs before lock:** scientific execution begins only after direct-source promotion and architecture sign-off.
21. **Exact-clean closure target:** every quantitative obligation targets `1.0000`; every nonnumeric obligation targets a clean pass. The `0.95` threshold is a hard failure floor, not the scientific objective.

## 4. Type constitution

The proof begins by preventing the identifications that caused earlier drift.

| Symbol/object | Type | Must not be silently identified with |
|---|---|---|
| CIF | source condition / field of possibility | RFL memory, calibrated physical field, next-cycle CIF |
| QV | action/compression/selection operator | background time, an already physical medium, a fitted damping term |
| RFL | manifest relational lattice | CIF, memory alone, spacetime assumed in advance |
| \(d\) | kernel recursion depth | physical time, event count, body number |
| \(e\) | event-order label | physical duration |
| \(r\) | route or relation label | source label, body label, time |
| \(N\) | finite body count | recursion depth or universe-cycle count |
| \(c\) | universe-cycle index | physical time inside a universe |
| \(\tau\) | derived physical clock | depth \(d\) or an external time coordinate |
| \(M\) | qualified persistent memory | complete RFL state or next effective CIF |
| \(\Pi\) | derived sector partition | an observational label imposed in advance |

The architecture permits a prephysical logical order before the Big Implosion. It does not permit physical time or a physical event before the Big Implosion.

## 5. Primitive action, kernel, and completion

### 5.1 Primitive action

The First Action is

\[
\mathcal A_0:\quad \mathrm{QV}(\mathrm{CIF})\longrightarrow \mathrm{RFL}.
\]

It is an ontological/constructive action, not yet the first physical event. The proof must explain how its output supplies the base relational structure from which the kernel and its physical interface are derived.

### 5.2 Base kernel

The repository reconstructs the canonical kernel skeleton as

\[
K_f(t)=
\sum_{j\in J_K}
\delta^{-d(j)}
e^{-\alpha d(j)t}
f_j(t),
\qquad d:J_K\to\mathbb N_0.
\]

This exact formula is **pending direct P29/P30 verification**. Until then, the architecture locks only:

- a typed source-owned index set;
- a depth map that is not physical time;
- separately typed zero-depth and positive-depth sectors;
- kernel functions that may not be selected to match a later target;
- coefficients that must be source-derived before any public comparison.

If the direct manuscript differs, the direct formula replaces this forensic reconstruction and the dependency graph remains unchanged.

### 5.3 Terminal N-body carrier

The reconstructed carrier is

\[
\mathbf C_N=
