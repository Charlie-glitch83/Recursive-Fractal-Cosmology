# RFC Canonical Proof Lock v3.0 — Source-Gated

**Scientific execution:** locked off  
**RFCU2 continuation:** prohibited  
**Promotion condition:** direct verification of all canonical source bytes

## Locked scientific order

1. `(CIF, QV, RFL)` is the ordered primitive triad.
2. `QV(CIF) → RFL` is the First Action.
3. The First Action is prephysical; it is not the Big Implosion.
4. The source-owned base kernel is derived after the First Action.
5. Recursion depth, event order, body count, route, cycle index, and physical time are different types.
6. The revised N-body construction is attached by typed composition at the terminal end of the base kernel.
7. The completed kernel is
   \[
   \widehat{\mathcal K}_N
   =\operatorname{Complete}_{NB}(K_f,\mathbf C_N,\mathsf{Act},\mathsf{Spec}),
   \]
   never `K_f` plus a second downstream N-body dynamics.
8. Every applicable later module inherits the completed kernel.
9. Inapplicable components are dormant and have zero backreaction.
10. Activation requires a route-local witness and a complete specialization certificate.
11. Protected prior structure is preserved under promotion, closure, refinement, and reopening.
12. Adding one body adds exactly \(2N\) directed lanes.
13. Lane growth, closure growth, and strict solution growth are separate results.
14. The proof establishes unbounded lawful finite manifestation without assuming an actually infinite material state.
15. QV-driven ordered persistent change must generate the RFC clock; no background physical time is assumed.
16. A minimal internal physical interface is derived before physical history, but it is not itself a prior physical event.
17. The Big Implosion is the sole first physical event and sets the physical time origin.
18. Dark matter and dark energy share Big-Implosion/triadic ancestry but require separate laws, roles, and observables.
19. Internal physicalization, conventional-unit calibration, prediction freeze, and public comparison are distinct stages.
20. The Big Rip is the terminal physical event.
21. Terminal RFL state, qualified memory, and the next effective CIF remain distinct.
22. The next cycle begins from qualified memory through a new effective CIF, First Action, completed kernel, and Big Implosion.

## Locked provenance rules

23. Every generative formula must have a source, type, birth record, and acyclic ancestry.
24. Historical target values cannot supply equations or constraints.
25. An unresolved coefficient stays symbolic unless independent source-owned constraints determine it.
26. Imported standard equations are comparison objects until an RFC derivation earns them.
27. Public data remain sealed through Module O, except a direct-source-authorized calibration anchor used only at O.
28. Module P compares the frozen packet without retuning.
29. A failed result can define a boundary, falsifier, or negative control; it cannot parent a replacement law.
30. The A7 post-hoc factor constructors are permanently quarantined.
31. Internal consistency, finite computational replay, conditional physical consequence, quantitative prediction, and empirical support are different claim levels.

## Locked closure-score rules

32. Every quantitative obligation targets `1.0000`; every categorical, logical, or theorem obligation targets `CLEAN_PASS`.
33. The `0.9500` threshold is the hard failure floor, not the target and not permission to round a residual into exact closure.
34. Scores are componentwise. No mean, aggregate, or composite score may conceal an individual component below `0.9500`.
35. `EXACT_PASS` requires `1.0000` at the declared precision and zero residual for an exact identity.
36. `CLEAN_PASS` requires every declared nonnumeric obligation to hold with no unresolved condition inside scope.
37. A result in `[0.9500,1.0000)` is `PASS_WITH_RESIDUAL`, not an exact or clean closeout; every residual and downstream limitation must be recorded before parentage is decided.
38. Any individual component below `0.9500` is `HARD_FAIL_TRIAD_AUDIT` and immediately stops that branch.
39. A hard-stop audit checks source fidelity; CIF/QV/RFL role separation; First Action order; full kernel inheritance; terminal N-body activation, dormancy, no-backreaction, and no-loss; module ownership; type integrity; and triad underutilization before implementation or precision is blamed.
40. A correction is source-first and target-blind, and replay starts from the last uncontaminated frozen checkpoint.
41. If no triad misrepresentation, omission, underutilization, metric defect, or implementation defect is found and the score remains below `0.9500`, the result remains a genuine unresolved failure.
42. Old failures have zero generative authority: they cannot supply outcomes, targets, constants, formulas, premises, state packets, or replacement laws.

## Locked A–Q order

| Range | Ownership |
|---|---|
| A–E | sources → triad → kernel → terminal N-body completion → lawful manifestation |
| F–G | endogenous order/clock → minimal internal physical bridge |
| H | Big Implosion, first physical event |
| I–N | sector bifurcation → evolution → geometry/structures → full-universe synthesis |
| O | quantitative closure, permitted calibration, prediction/falsifier freeze |
| P | blind public empirical comparison |
| Q | Big Rip, qualified memory, next effective CIF, recurrence |

No module may require an output owned by one of its descendants.

## Current source gate

The exact bytes of the following files must be directly read and hash-matched before this lock becomes canonical:

- Presentation 29 revised raw LaTeX and metadata;
- Presentation 30 and Appendix J;
- revised N-body proof and metadata;
- `Module Plans.zip`;
- `RFC Canonical Master Architecture v1.0.md`;
- `RFC Canonical Architecture v1.1 Bundle.zip`.

Until promotion, this lock authorizes architecture review and source reconciliation only. It authorizes no scientific run.
