# Module A Export \(H_A\) v1.0

**Export ID:** `RFC-H_A-v1.0`  
**Status:** `FROZEN`  
**Mode:** `GENERATION_SEALED`  
**Certificate:** `TEC_A = 1.0000/CLEAN_PASS`  
**Module B:** `AUTHORIZED`

## Export

\[
\boxed{
H_A
=
\left(
S_A,
\Sigma_A,
\mathcal R_A,
\mathcal E_A,
M_A,
\mathcal S_A,
\Pi_A,
F_A,
C_A
\right).
}
\]

## \(S_A\) - triadic and kernel state

- primitive triad: CIF, QV, RFL;
- canonical First Action: \(QV(CIF)\to RFL\);
- typed return:
  \[
  RFL^{\mathrm{manifest}}
  \to
  M_{\mathrm{rec}}^{\mathrm{latent}}
  \dashrightarrow
  CIF_{\mathrm{next}}^{\mathrm{conditioned}};
  \]
- recursive-depth, recurrence-stage, body-count, route, event, scale, and time types remain distinct;
- base kernel:
  \[
  K_f(t)=\sum_{j=0}^{\infty}\delta^{-j}e^{-\alpha jt}f_j(t);
  \]
- symbolic constraints: \(\delta>1\), \(\alpha>0\);
- no historical numerical packet value is admitted;
- exact convergence, tail, stability, and conditional derivative certificates;
- normalized recursive-depth entropy as mathematical inherited-influence dispersion.

## \(\Sigma_A\) - uncertainty state

Every downstream object must carry uncertainty type, covariance/interval representation, derivation source, transformation law, validity range, and freeze status.

Where a justified differentiable map exists:

\[
\Sigma_{\mathrm{out}}
=
J\Sigma_{\mathrm{in}}J^\top.
\]

Uncertainty is not permission to tune.

## \(\mathcal R_A\) - completed relational carrier

The generic constituent schema carries:

- identity and type;
- state and dimensions;
- directed relation ports;
- triadic packet;
- witness and event status;
- memory and scale;
- uncertainty and provenance.

For finite \(N\):

\[
|L_N|=N(N-1),
\qquad
|L_{N+1}|-|L_N|=2N.
\]

The terminal route kernel is:

\[
K_{[r],N}(t)
=
\sum_{j=0}^{\infty}
\delta^{-j}e^{-\alpha jt}
\Phi_{j,[r],N}(t).
\]

The completed carrier is:

\[
\widehat{\mathcal K}_N
=
\operatorname{Complete}_{NB}
\left(
K_f,
\mathbf C_N,
\mathsf{Witness},
\mathsf{Event},
\mathsf{Memory},
\mathsf{NoLoss},
\mathsf{Specialize}
\right).
\]

It consists of unique maximal noncollision charts, witnessed event charts, branchwise reopening, and explicit obstruction where no lawful continuation exists.

## \(\mathcal E_A\) - event grammar

The event lift returns exactly one of:

- obstruction;
- unique continuation;
- gauge-equivalent branch family;
- independent multiroute family.

It never selects an unwitnessed branch or uses a public target as a selector.

## \(M_A\) - no-loss memory

The protected signature has 18 fields. Quotienting is lawful only when every protected field agrees.

Memory satisfies:

\[
\operatorname{Decode}_M
\circ
\operatorname{Encode}_M
=
\operatorname{id}.
\]

## \(\mathcal S_A\) - promotion, reopening, dormancy

Promotion is a split monomorphism:

\[
\Pi_\downarrow\Pi_\uparrow
=
\operatorname{id}
\]

on the protected state.

Dormant components retain ancestry but have exactly zero contribution and zero backreaction. Activation requires both a witness and a specialization certificate.

## \(\Pi_A\) - provenance and permissions

Downstream modules may instantiate source-owned domain laws and witnessed modal bases. They may not:

- modify \(H_A\);
- fit or retune source fields;
- create a duplicate N-body engine;
- identify depth with physical time;
- select a branch from a target;
- erase protected information;
- execute a physical event without a law and witness.

## \(F_A\) - falsifiers

Every component targets `1.0000/CLEAN_PASS`. Any component below `0.9500` is a hard stop and triad-representation/underutilization audit. No average may hide a failed component.

## \(C_A\) - certificate and authorization

\[
\boxed{
TEC_A=1.0000/\mathrm{CLEAN\_PASS}.
}
\]

Module B is authorized to begin deriving the Big Implosion.

Module A supplies no background physical time and has not executed a physical cosmology event. Module B must derive the Big Implosion as the sole first physical cosmological event, including physical-time origin, geometry/field genesis, conservation closure, and the common ancestry and partition of the ordinary, radiative, compression-relic, and dissipative-tail sectors.
